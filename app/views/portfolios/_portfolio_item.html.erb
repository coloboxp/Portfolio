<p><h2><%= link_to portfolio_item.title, portfolio_show_path(portfolio_item.id) %></h2></p>
<p><%=portfolio_item.subtitle%></p>
<p><%=portfolio_item.body%></p>
<%= image_tag portfolio_item.thumb_image if !portfolio_item.thumb_image.nil? %>

<% if logged_in?(:site_admin)  %>
<%= link_to "Edit", edit_portfolio_path(portfolio_item.id)  %> |
<%= link_to 'Delete', portfolio_path(portfolio_item.id), method: :delete, data: { confirm: 'Are you really sure?' } %></td>
<% end %>